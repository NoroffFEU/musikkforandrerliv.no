---
layout: default
title: Home
---

{% assign bgImage = "/assets/placeholder-images/IMG_3779.webp" %}

<section style="background-image: url('{{ bgImage }}')" class="-mt-32 h-screen bg-cover bg-center flex flex-col justify-end items-center">
  <div class="flex flex-col text-center mb-16 gap-7">
    <h1 class="font-justAnotherHand text-white text-8xl">
      MOZIKA MANOVA FIAINANA
    </h1>

    <h2 class="font-sans text-white text-3xl">
      MUSIKK FORANDRER LIV - MUSIC CHANGES LIVES
    </h2>
  </div>
</section>

<div class="flex justify-center my-[100px]">
  <div class="max-w-[960px] flex flex-col items-center">
    <h2 class="text-center text-6xl mb-6">{{ home.history.title }}</h2>
    <div class="max-h-20 overflow-hidden">
      <p class="text-center text-lg">{{ home.history.short_description }}</p>
    </div>
    <button class="reg-button my-10 bg-primary-green text-white" id="readMoreBtn">
      Read More
    </button>
    <div class="max-h-[576px] overflow-hidden">
      <img src="{{ home.history.image }}" alt="{{ home.history.title }}">
    </div>
  </div>
</div>

{% include 'staff.liquid' %}

{% include 'home-content.liquid' %}

{% include 'simple-gallery.liquid' %}

{% include 'simple-events.liquid' %}